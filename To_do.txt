- Use pots which include a button.
    The button could, for example, toggle whether sound output is muted or not.
    https://core-electronics.com.au/panel-mount-10k-log-potentiometer-w-on-off-switch-10k-log-w-switch.html
    amazon.com (in the USA not in Australia) sells pots with switches.

- Move all functions that use a HANDLE into AutoHANDLE so that there's no need for the AutoHANDLE::get() function.

- [partially implemented] Ensure that the display brightness is set correctly when the input changes between HDR and non-HDR.
  Note that the LG C2 records individual brightness levels for HDR and non-HDR inputs. The Windows app should resend the brightness level whenever Windows changes between using HDR / non-HDR.
